name: Github actions demo 

on: [push]

jobs: 
  my-first-job:
    runs-on: ubuntu-latest
    steps:
      - name: hello
      - run: |
          echo "Hello, man"
      - name: run python script 
        run: |
          pip install -r requirements.txt 
          python ci.py 